# Recommended Python version: 3.11 or 3.12
# This file contains the packages required to run the core app without build errors.

streamlit==1.53.0
numpy==2.4.5
pandas==2.3.3
scikit-learn==1.7.2
opencv-python==4.9.0.80
Pillow==12.2.0
matplotlib==3.10.9
seaborn==0.13.2
SpeechRecognition==3.10.1
requests==2.31.0
pydub==0.25.1
imageio-ffmpeg==0.6.0
# pyaudioop may be needed for certain Python versions to support pydub audioop compatibility
# If you get audio conversion import errors, install: pyaudioop
# pyaudioop==0.1.0
tqdm==4.66.4

# Optional packages
# Uncomment the following line only if you need TensorFlow-based image model inference
# and are running on a supported Python version (3.11 or 3.12).
# tensorflow-cpu==2.15.0
